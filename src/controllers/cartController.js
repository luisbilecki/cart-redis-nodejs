const getCompleteCart = (req, res, redis) => {
  return res.json('ok');
};

const addCartItem = (req, res, redis) => {
  return res.json('ok');
};

const updateCartItem = (req, res, redis) => {
  return res.json('ok');
};

const deleteCartItem = (req, res, redis) => {
  return res.json('ok');
};

const clearCart = (req, res, redis) => {
  return res.json('ok');
};

module.exports = {
  getCompleteCart,
  addCartItem,
  updateCartItem,
  deleteCartItem,
  clearCart,
};
